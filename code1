#include<stdio.h>
int main()
{
	int num;
	int isprime=0;
	printf("enter number");
	scanf("%d",&num);
	for(int i=2;i<10;i++)
	{
		if(num%i==0)
		{
			 isprime++;
		}
	}
	if(isprime>1)
	{
		printf("%d is not a prime number",num);
	}
	else
	{
		printf("%d is  a prime number",num);
	}
	
}
